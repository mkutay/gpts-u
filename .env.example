# Environment variables for Usuyus Discord Bot
# Copy this file to .env and fill in your actual values

# ==============================================================================
# OpenAI Configuration
# ==============================================================================

# Your OpenAI API key - Get from https://platform.openai.com/api-keys
# Required for both training and running the bot
OPENAI_API_KEY=sk-your-openai-api-key-here

# Fine-tuned model ID - You'll get this after running the training process
# Leave empty until you complete training, then update with the model ID
# Example: ft:gpt-3.5-turbo-1106:your-org::AbCdEfGh
FINE_TUNED_MODEL_ID=

# ==============================================================================
# Discord Configuration
# ==============================================================================

# Your Discord bot token - Get from https://discord.com/developers/applications
# 1. Create a new application at https://discord.com/developers/applications
# 2. Go to the "Bot" section and create a bot
# 3. Copy the token here
DISCORD_BOT_TOKEN=your-discord-bot-token-here

# The Discord channel ID where the bot should respond
# Right-click on a channel in Discord → "Copy Channel ID" (Developer Mode must be enabled)
CHANNEL_ID=your-discord-channel-id-here

# The target username that the bot should mimic
# This should match the username in your training data
TARGET_USER=your-target-username-here

# The system prompt for the bot - defines the bot's personality and behavior
# This is a multiline string that defines how the bot should behave
SYSTEM_PROMPT=your-system-prompt-here

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Base model to use for fine-tuning (default: gpt-3.5-turbo)
# BASE_MODEL=gpt-3.5-turbo

# Custom suffix for your fine-tuned model (default: chat-model)
# MODEL_SUFFIX=chat-model

# ==============================================================================
# Setup Instructions
# ==============================================================================

# 1. Get OpenAI API Key:
#    - Go to https://platform.openai.com/api-keys
#    - Create a new API key
#    - Make sure you have credits for fine-tuning

# 2. Create Discord Bot:
#    - Go to https://discord.com/developers/applications
#    - Create "New Application"
#    - Go to "Bot" section → "Add Bot"
#    - Copy the token
#    - Enable "Message Content Intent" under "Privileged Gateway Intents"

# 3. Train the Model:
#    - Run: deno task train
#    - Wait for completion (can take 10-30 minutes)
#    - Update FINE_TUNED_MODEL_ID with the returned model ID

# 4. Invite Bot to Server:
#    - Go to OAuth2 → URL Generator
#    - Select "bot" scope
#    - Select permissions: "Send Messages", "Read Message History"
#    - Use the generated URL to invite your bot

# 5. Start the Bot:
#    - Run: deno task server
